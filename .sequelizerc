const path = require('path')

module.exports = {
   config: path.resolve(__dirname, 'source', 'database', 'config.json' ),
   'migrations-path': path.resolve(__dirname, 'source', 'database', 'migrations'),
   'seeders-path': path.resolve(__dirname, 'source', 'database', 'seeders')
}